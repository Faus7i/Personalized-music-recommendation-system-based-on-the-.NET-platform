# Debug Session: music-platform-bugs [CLOSED]

## 问题范围

- 歌曲封面图加载异常
- 推荐歌曲“换一批”失效
- 喜欢歌单重复收藏 / 全局收藏状态不同步
- 歌曲无法正常播放
- 需要补充音乐数据集说明

## 初始假设

1. 封面图和音频资源使用的外部 URL 存在失效、限流或跨域不可用情况，导致浏览器资源加载失败。
2. “换一批”按钮虽然触发了刷新方法，但后端推荐接口没有基于已展示歌曲做排除或随机偏移，所以返回内容重复。
3. 喜欢歌单重复问题来自偏好表、歌单表或 UI 状态层缺少去重保护，导致同一歌曲可被多次写入或多次展示。
4. 播放失败来自音频源不可访问、格式不兼容，或播放器只切换了状态但没有拿到可播放的真实音频地址。
5. 数据集说明缺失是因为当前曲库主要依赖种子数据写入数据库，尚未形成统一文档与统计输出。

## 调试计划

1. 为封面图、播放、推荐刷新、喜欢操作增加最小化运行时埋点
2. 启动系统并复现问题
3. 基于证据确认根因
4. 进行最小修复
5. 修复后再次验证并整理数据集说明

## 运行时证据与判定

| ID | 假设 | 状态 | 证据摘要 |
|----|------|------|----------|
| A | 封面图 / 音频是外链失效导致无法加载 | ❌ 否定 | `.dbg/trae-debug-log-music-platform-bugs.ndjson` 第 4-5 行显示后端实际返回的是 `https://placehold.co/300x300` 和空 `AudioUrl`，不是种子数据中的真实资源。 |
| B | “换一批”点击交互没触发或前端没有发请求 | ❌ 否定 | 第 6 行记录已点击刷新，第 7-8 行记录后端重新返回结果，说明交互与请求都正常触发。 |
| C | 推荐列表重复来自后端候选池过小、排序固定且未显式排除已展示歌曲 | ✅ 确认 | 第 4 行与第 7 行 `candidateSongIds` 完全一致，`returnedSongIds` 也一致；说明每次刷新都在同一小候选池内按固定分数返回。 |
| D | 播放失败来自播放器逻辑没切换，而不是点击失效 | ✅ 确认 | 第 9 行显示首页点击播放已拿到歌曲，但 `audioUrl` 为空；第 10 行紧接着触发 `Audio error event fired`。 |
| E | 喜欢歌单重复的主要原因是后端重复写入同一偏好记录 | ⏳ 待补充 / 部分否定 | 第 11 行显示首次喜欢走 `insert-new`，当前未观察到同歌再次插入；但前端在喜欢后仍继续显示“喜欢”按钮，说明全局收藏状态未同步禁用。 |

## 当前根因结论

1. 曲库数据库未被新的种子数据回填，仍保留旧占位封面和空音频地址。
2. 推荐服务的刷新机制只有“重新请求”，没有“排除已展示歌曲”的能力；同时现有有效候选池数量偏少。
3. 前端没有统一的全局喜欢状态，导致已喜欢歌曲在不同场景仍显示可重复点击的“喜欢”入口。

## 修复结果

1. 曲库种子改为回填更新，旧占位封面和空音频地址被替换为真实可访问资源。
2. 推荐接口支持 `excludeSongIds`，首页会记录已展示歌曲并在“换一批”时排除已出现内容。
3. 新增 `PreferenceState` 统一管理全局喜欢 / 不喜欢状态，首页、详情页、我喜欢的音乐页同步禁用重复收藏。
4. 喜欢歌单查询使用 `DistinctBy` 去重，避免同一歌曲重复展示。
5. 封面展示增加前端兜底图逻辑，即使单个远程图片失败也会自动切换到可访问的生成封面。

## 回归验证摘要

1. 首页封面请求已切换为可访问的真实图片地址，不再依赖旧的 `placehold.co` 占位图。
2. “换一批”连续点击后返回不同推荐集合，前后两批歌曲名称不重复。
3. 首页点击喜欢后，“我喜欢的音乐”数量立即从 `4` 变为 `5`，并在喜欢页只展示 5 首去重后的歌曲。
4. 音频播放已能正常触发加载与播放，播放器可成功试听推荐歌曲。
